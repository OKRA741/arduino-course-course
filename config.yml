title: Arduino Course
description: This is a very easy course.
template:
  repo: arduino-course-template
  name: arduino-course
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
